name: Deploy Anchor (devnet)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Devnet deploy temporarily disabled
        run: |
          echo "Anchor devnet deployment is currently disabled."
          echo "No Solana / Rust / Cargo is being run in this workflow."
